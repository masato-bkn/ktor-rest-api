# テスト用設定ファイル
# modules を空にして、本番の module() が自動ロードされるのを防ぐ。
# testApplication の application {} ブロックで明示的にモジュールを構成する。
ktor {
    deployment {
        port = 8080
    }
    application {
        modules = []
    }
}
