-- =============================================================================
-- V2: users テーブル作成
-- =============================================================================
-- ユーザー管理の基本テーブル。
-- Exposed の Tables.kt (Users オブジェクト) と対応する。
-- カラムの型・制約は Tables.kt の定義と一致させる必要がある。
--
-- 対応する Exposed 定義: com.example.db.Users
-- =============================================================================

CREATE TABLE users (
    id SERIAL PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    email VARCHAR(255) NOT NULL
);
